# План внедрения нового дизайна админ-панели (ui_solid1)

**Задача:** T-376 · ветка `task/T-376-admin-redesign` · база `origin/main` (4c1cee0)
**Источник дизайна:** `ui_solid1.zip` (2026-09-25) — два `.dc.html` живых прототипа: `Сотрудники` и `Финансы дня`.
**Принцип:** редизайн-эволюция. Продакшн уже использует ту же дизайн-систему (сайдбар `#143e35`, золото `#d8b977`, `.panel`, `.eyebrow`). Меняем раскладку и паттерны, логику/эндпоинты переиспользуем.

## Что в дизайне (8 поверхностей)

| # | Экран | Статус в продакшене | Тип работы |
|---|---|---|---|
| 1a | **Сотрудники** — реестр | `employees.html/js` | Редизайн (frontend-only) |
| 2a | **Бухгалтер · Финансы дня** | `accountant.html/js` | Реорг + рескин |
| 2b | **Ведомость месяца** (сотрудник × день) | частично в `accountant.js` | Расширение |
| 3a | **Закуп · Шох** (моб. приложение) | **нет** | Новая фича целиком |
| 4a / 7 | **Учредитель** (тел. + ПК) | `founder.html/js` | Рескин + дивиденды-флоу |
| 5a | **Кассир** — день в цифрах | `index.html`/`app.js` | Рескин |
| 6a | **Директор** — телефон + AI в центре | `director.html/js` | Реорг под AI-first |

### Единая дизайн-система (закрепить в `style.css`)
- Цвета: сайдбар `#143e35`, акцент `#d8b977`, фон `#e9ebe4`, карточка `#f6f7f2`, скругление 18px.
- `font-variant-numeric: tabular-nums` на всех суммах; Inter.
- Паттерны: **stat-карточки как фильтры**, боковой **drawer** для create/edit, группировка по функц. группам, бейдж **ДЕМО** только где данных реально нет (проходы Hikvision ресторана пока не подключены), футер «Время Ташкента · UZS».

## Фазы (всё вместе, поэтапно)

### Фаза 1 — СЕГОДНЯ · десктопная админка бухгалтера
- **1.1 Сотрудники (1a)** — flagship, frontend-only. Порт прототипа на живой API `GET /api/accountant/staff`:
  - Убрать date-input → **date-nav** (‹ › · Сегодня · Вчера) с меткой дня.
  - **5 stat-карточек-фильтров**: К начислению за день · На месте (N/total + прогресс) · Опоздали · Не пришли · Требуют внимания. Клик = фильтр реестра.
  - Реестр **по группам** (Управление/Кухня/Обслуживание зала/Бар/…), колонки: Сотрудник (аватар-инициалы) · Первый вход · Статус (pill) · Ставка за смену · Начислено. Поиск + табы по группам.
  - Боковой **drawer** create/edit/delete с полем «Причина изменения» (идёт в историю) — заменяет inline-кнопки и dblclick. API: POST/PATCH/DELETE `/api/accountant/employees` уже есть.
  - «Начислено» считаем клиентски по статусу (missing→0, unlinked→—, иначе rate), как в прототипе.
- **1.2 Финансы дня (2a)** — рескин + порядок панелей как в макете: Сменные (выдача за вчера) → Журнал списаний → Закуп·подотчёт → Оклады (частичная выплата) → Деньги на расходы → Проверки → Долги → Резервы → Дивиденды недели. Логика/эндпоинты те же.
- **1.3** Вынести общий CSS дизайн-системы в `style.css` (переменные, sidebar, `.stat-card`, `.drawer`, footer), чтобы Фазы 2–3 переиспользовали.

### Фаза 2 — не сегодня
- Ведомость месяца 2b (двусторонняя связь с 2a), рескин Кассира 5a, единый стиль экранов Учредителя 4a/7.

### Фаза 3 — отдельные фичи
- **Закуп · Шох 3a**: новый мобильный модуль (покупка → точка → товар+фото → кол-во/цена → баланс Шоха в 2a), геймификация. Директор 6a — AI-чат в центре.

## Риски / открытые вопросы
- Прототипы на мок-данных (`ROSTER`, смоделированные проходы). При порте — реальные эндпоинты; ДЕМО-бейдж оставить только для непокрытых данных.
- `projects/retro-app` на `codex/cashier-dashboard` с незакоммиченными правками — работаем в отдельном worktree `retro-app-T-376`, его не трогаем.
- Тесты: после каждого этапа гнать локально (`pytest`), CI self-hosted runner может быть офлайн — опираемся на локальный прогон.
